{% extends 'layout.twig' %}

{% block body %}

                <form method="post" action="/app/AddDocs" id="add" enctype="multipart/form-data" id="add">
                    <input type="text" name="titre" id="titre" placeholder="titre de votre document" required="required">
                    <select name="domain" id="domain-select" required="required">
                        <option value="">--Choisissez un domaine--</option>
                        {% if domaines %}
                            {% for domaine in domaines %}
                                <option value="{{ domaine }}">{{ domaine }}</option>
                            {% endfor %}
                        {% endif %}
                        <option value="Autre">Autre</option>
                    </select>
                    <div id="otherDomaine">
                        <input type="text" name="domaine" placeholder="Définir un domaine">
                    </div>
                    <textarea name="description" id="textarea" rows="5" cols="33" placeholder="Description" required="required">
                    </textarea>
                    <input type="file" name="fileToUpload" id="fileUpload" required="required">
                    <input type="button" id="valider" value="valider">
                    <div id="sub">

                    </div>
                </form>

{% endblock %}